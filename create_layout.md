* Click 'add directory'
  * Give it a name and select the rows and columns number (this can be changed later)
  * Click 'Accept'
* The editor goes to the newly created directory. There are no items.
* Let's create an item to go back to the main directory:
  * Click 'add item'
  * Select 'ChangeDir' and click 'Accept'
  * Click on the newly created item to edit it.
  * Select what directory do you want to go when click this item (default is `_main_`)
  * Customize the state of the item (text to show, text color, background color, text size and the icon). Let's create an icon:
    * Click 'add icon' button.
    * Give a name to the icon. The app will use the base64 code to use the image, there are two options to upload an image:
      * Drag the file to the green area. When dropped the image, the base64 code of the file will be shown in the textarea.
      * Write directly the base64 code in the textarea.
    * Click 'Accept'. Now this icon is available in the select menu of the state properties.
* Let's create an item to xxxx before shutdown the computer


Smartphonea edo tableta aginte panel moduan erabiliz, ordenagailua kontrolatzeko sistema da ideckia. Panela guztiz pertsonalizagarria da, nahi adina karpeta sortu daitezke eta karpeta bakoitzean nahi adina elementu jarri daitezke  zure edukia antolatuta edukitzeko. Jokogintzarako, streaming-erako, software garapenerako, etxeko eta hainbat modutako automatizazioetarako erabili daiteke.

Ideckia is a system to use your smartphone or tablet as a control panel to send orders to your computer. You can create a customized panel, as many directories and items per directory as you want to structure your content to fit your needs. You can use it or automations in gaming, streaming, software development, home...